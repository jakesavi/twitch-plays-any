def getToken(USER, PASS):
    #TODO This function should fetch a token from a user and have them input their credentials for twitch. If this works. we can then get their token to latch
    #To their chat.
    