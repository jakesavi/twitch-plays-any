# twitch-plays-any  
A program that forms token for the twitch api and latches to chat to do twitch plays!  
##TODO  
First.) set up token getting program. This will be a semi-easy part of the code.  
Second.) We should look for ways to set hot keys to JSON blobs. This will allow us to make it so twitch CAN play any game. However, its on the user to form these.  
(Second post operation: Maybe make a gui that adds controls and formats a JSON blob for them?)  
Third.) Latch. We need to then program the bot to latch to these json format hot keys for each key in the json and make it so that it creates hot keys to the users pc.   
